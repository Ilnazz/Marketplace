<ui:MenuItem
    x:Class="Marketplace.Views.OrdersMenuItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:Marketplace.ViewModels"
    d:DataContext="{d:DesignInstance viewmodels:OrdersMenuItemView}"
    Command="{Binding NavigateToOrdersPageCommand}"
    Visibility="{Binding UserHasPermission, Converter={StaticResource BoolToVisibilityConverter}}"
    mc:Ignorable="d">

    <ui:MenuItem.DataContext>
        <viewmodels:OrdersMenuItemVm />
    </ui:MenuItem.DataContext>

    <ui:MenuItem.Header>
        <StackPanel>
            <ui:SymbolIcon Symbol="Box16" />
            <TextBlock Text="Заказы" />
        </StackPanel>
    </ui:MenuItem.Header>
</ui:MenuItem>
