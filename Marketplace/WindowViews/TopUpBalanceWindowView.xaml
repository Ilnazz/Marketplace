<UserControl
    x:Class="Marketplace.WindowViews.TopUpBalanceWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:windowvms="clr-namespace:Marketplace.WindowViewModels"
    Width="200"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style BasedOn="{StaticResource TextBoxInErrorStyle}" TargetType="{x:Type ui:TextBox}" />
    </UserControl.Resources>

    <StackPanel>
        <TextBlock
            Margin="0,0,0,5"
            FontSize="16"
            Text="Введите сумму пополнения" />
        <ui:TextBox Text="{Binding TopUpSum, UpdateSourceTrigger=PropertyChanged}" />
        <ui:Button
            Margin="0,10,0,0"
            HorizontalAlignment="Stretch"
            Command="{Binding TopUpBalanceCommand}"
            Content="Пополнить" />
    </StackPanel>
</UserControl>
