<UserControl
    x:Class="Marketplace.WindowViews.AddEditDeliveryPointWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="350"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style BasedOn="{StaticResource TextBoxInErrorStyle}" TargetType="{x:Type ui:TextBox}" />
    </UserControl.Resources>

    <StackPanel Margin="10">
        <TextBlock
            Margin="0,0,0,5"
            FontSize="15"
            Text="Название" />
        <ui:TextBox ClearButtonEnabled="False" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock
            Margin="0,10,0,5"
            FontSize="15"
            Text="Адрес" />
        <ui:TextBox ClearButtonEnabled="False" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" />
        <ui:Button
            Margin="0,10,0,0"
            HorizontalAlignment="Right"
            Command="{Binding SaveCommand}"
            Content="Сохранить" />
    </StackPanel>
</UserControl>
