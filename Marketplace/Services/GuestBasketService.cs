using Marketplace.Database.Models;

namespace Marketplace.Services;

public class GuestBasketService
{
    public void AddProductToBasket(Product product)
    {
        //var testProp = new SettingsProperty("TestProperty");
        //testProp.PropertyType = typeof(IEnumerator<ProductInBasket>);
        //Settings.Default.Properties["TestProperty"]
        //Settings.Default.Properties.Add(testProp);
    }
}
